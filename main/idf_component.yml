dependencies:
  espressif/led_strip: "^2.5"
  espressif/mdns: "^1.4"
  # iot_button — debounced GPIO button driver with single/long-press detection.
  # Drives the on/off + next + prev buttons; long-press on power triggers
  # factory_reset_full() (Phase 8).
  espressif/button: "^4.1"
  idf:
    version: ">=5.3"
