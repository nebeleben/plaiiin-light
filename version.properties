# PlaiiinLightOS version manifest. Read by scripts/build.sh and injected
# as Kconfig overrides (CONFIG_PLAIIIN_FIRMWARE_VERSION / API_VERSION).
# Dot-separated numeric only. Bump firmware.version on every release.
firmware.version=2.1.2
api.version=2.1.0
