PHYSICAL FORM: tower (cylinder). The {w}x{h} pixel grid is wrapped around a cylinder, so column {wmax} sits physically next to column 0 — there is no left or right edge. Horizontal motion must be seamless across that seam: derive an angle from x as (x / {w}) and drive it with sinLUT/cosLUT so the pattern meets itself. y is vertical (0 = top, {hmax} = bottom) with hard top and bottom edges.
